<div class = "secret">
    <h1>Secret Number</h1>

    <% if @number >= 1 && @number <=10 %>
        <p>You choose <%= params[:num]%> and it is <%= @answer %></p>
        <p>Correct answer is <%= @secret_number %></p>
    <% else %>
        <p>You answer is "<%= params[:num] %>" and it is not between 1 to 10</p>
    <% end %>

    <form action = '/secretnumber'>
        <button>Play again</button>
    </form>
</div>