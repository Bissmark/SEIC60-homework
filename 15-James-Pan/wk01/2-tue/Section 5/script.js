let isMovable = false
let shouldMove = false
let shouldStop = true
if (isMovable === true ) {
    if (shouldMove === true) {
        console.log('No problem')
    } else {
        console.log('Use sticky tape')
    }
} else {
    if (shouldStop === true) {
        console.log('Use WD-40')
    } else {
        console.log('No problem')
    }
}