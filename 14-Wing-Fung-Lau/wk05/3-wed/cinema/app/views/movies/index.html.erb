<h1>List of Movies</h1>
<% @movies.each do |movie| %>
    <% if movie.image.present?%>
        <%= link_to image_tag(movie.image, :title => movie.title, :class => 'thumbMovies'), movie %>
    <% else%>
        <%= link_to movie.title, movie %>
    <% end%>
<% end%>

