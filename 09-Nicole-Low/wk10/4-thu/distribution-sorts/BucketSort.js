function bucketSort(items) {

  // number of buckets (square root of the total items)
  const buckets = Math.sqrt(items.length)

  // want to know the range of numbers for the buckets


  

  return items;
}

module.exports = bucketSort;
